{
  // python path
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",

   // mypy
   // * Mypy Type Checker の利用が推奨されているが、機能しないときがあるので、従来の設定方法を使用
   "python.linting.mypyEnabled": true,
   "python.linting.mypyPath": "${workspaceFolder}/.venv/bin/mypy",

  // flake8
   "flake8.args": [
    "--max-line-length=88",
  ],
}